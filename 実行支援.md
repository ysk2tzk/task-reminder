# 実行支援 要件定義（MVP）

## アプリ情報

  項目           値
  -------------- -----------------------------------------------
  アプリ名       実行支援
  リポジトリ名   task-reminder
  PWA名          task-reminder
  実行環境       Vercel / Supabase / PWA / Google Calendar API

------------------------------------------------------------------------

# 1. 目的

-   指定時刻にタスクを通知する
-   完了するまで指定間隔で再通知する
-   完了したタスクをGoogleカレンダーへ実績として記録する
-   スキップも履歴として残す（Googleカレンダーには登録しない）

------------------------------------------------------------------------

# 2. 基本仕様

-   単一ユーザー利用
-   タスクは削除しない
-   タスクは有効／無効で管理する
-   通知間隔はタスク単位
-   完了時のみGoogle Calendarへ登録
-   スキップはGoogle Calendarへ登録しない

------------------------------------------------------------------------

# 3. 状態

## タスク

-   有効
-   無効

## 実行履歴

-   pending
-   completed
-   skipped

状態遷移

    pending
    ├── completed
    └── skipped

------------------------------------------------------------------------

# 4. DB設計

## tasks

  カラム                      内容
  --------------------------- -----------------------
  id                          PK
  title                       タスク名
  description                 説明
  schedule_type               once / daily / weekly
  scheduled_date              1回のみの日付
  scheduled_time              実行時刻
  weekdays                    曜日指定
  reminder_interval_minutes   通知間隔(分)
  is_active                   有効／無効
  created_at                  作成日時
  updated_at                  更新日時

## task_occurrences

  カラム                 内容
  ---------------------- -------------------------------
  id                     PK
  task_id                tasks.id
  scheduled_at           予定日時
  status                 pending / completed / skipped
  next_notification_at   次回通知日時
  completed_at           完了日時
  skipped_at             スキップ日時
  google_event_id        GoogleイベントID
  calendar_synced_at     同期日時
  calendar_sync_error    同期エラー
  created_at             作成日時
  updated_at             更新日時

## push_subscriptions

  カラム
  ------------
  id
  endpoint
  p256dh
  auth
  created_at
  updated_at

## app_settings

  カラム
  ----------------------
  id
  google_calendar_id
  google_refresh_token
  created_at
  updated_at

------------------------------------------------------------------------

# 5. 画面構成

1.  ホーム
2.  タスク一覧
3.  タスク登録
4.  タスク編集
5.  履歴
6.  履歴詳細
7.  設定

## ホーム

-   未完了かつ予定時刻超過のタスク（古い順）
-   今日これから実行するタスク（時刻順）
-   完了・スキップ済みは表示しない

操作 - 完了 - スキップ

## タスク一覧

-   作成日時降順
-   有効・無効とも表示

表示項目 - タスク名 - 実行条件 - 通知間隔 - 有効／無効

操作 - タップ → タスク編集 - 右下＋ → タスク登録

## タスク登録

入力項目 - タスク名 - 説明 - 実行条件 - 1回だけ - 毎日 - 曜日指定 -
通知間隔

## タスク編集

入力項目 - タスク名 - 説明 - 実行条件 - 通知間隔 - 有効／無効

## 履歴

表示順 - 完了日時・スキップ日時の新しい順

表示項目 - タスク名 - 予定日時 - 完了日時またはスキップ日時 -
完了時は予定との差

機能 - タスク名検索 - タップで履歴詳細

※絞り込み機能は設けない

## 設定

-   Google Calendar接続状態
-   Google Calendar再接続
-   Google Calendarテスト登録
-   Push通知状態
-   テスト通知
-   アプリ情報

------------------------------------------------------------------------

# 6. Google Calendar連携

completed のみ登録する。

登録内容

-   タイトル：タスク名
-   開始日時：completed_at
-   説明
    -   予定日時
    -   完了日時

skipped は登録しない。

------------------------------------------------------------------------

# 7. 通知仕様

-   予定時刻になると通知
-   未完了なら通知間隔ごとに再通知
-   completed または skipped で通知停止

------------------------------------------------------------------------

# 8. 画面遷移図

``` mermaid
flowchart TD

    subgraph TAB["下部タブ"]
        HOME[ホーム]
        TASKS[タスク]
        HISTORY[履歴]
        SETTINGS[設定]
    end

    TASKS -->|＋| CREATE[タスク登録]
    TASKS -->|タスク選択| EDIT[タスク編集]

    CREATE -->|登録 / 戻る| TASKS
    EDIT -->|保存 / 戻る| TASKS

    HISTORY -->|履歴選択| DETAIL[履歴詳細]
    DETAIL -->|戻る| HISTORY
```


---

# 9. 処理インターフェース仕様

本章では、処理を以下の2種類に分ける。

1. 画面から呼び出す処理
2. バックグラウンド・内部処理

実装方式はREST APIに固定しない。
Next.js Server Actions、Route Handlers、Supabaseクライアント、Supabase Edge Functions等から、適切な方式を選択する。

---

## 9.1 画面から呼び出す処理

### 9.1.1 ホーム表示用タスク取得

#### 用途
ホーム画面に表示する未完了タスクを取得する。

#### 入力
なし

#### 取得条件
- `task_occurrences.status = 'pending'`
- 以下の2グループを対象とする
  - 現在時刻以前の未完了タスク
  - 当日中かつ現在時刻より後のタスク

#### 並び順
1. 予定時刻を過ぎたものを `scheduled_at ASC`
2. 今日これからのものを `scheduled_at ASC`

#### 出力例

```json
[
  {
    "occurrence_id": "uuid",
    "task_id": "uuid",
    "title": "朝の薬",
    "description": "朝食後",
    "scheduled_at": "2026-08-10T07:00:00+09:00",
    "status": "pending",
    "reminder_interval_minutes": 5
  }
]
```

---

### 9.1.2 タスク一覧取得

#### 用途
タスク一覧画面に登録済みタスクを表示する。

#### 入力
なし

#### 取得条件
- `tasks`を全件取得
- 有効・無効の両方を含む

#### 並び順
- `created_at DESC`

#### 出力項目
- id
- title
- description
- schedule_type
- scheduled_date
- scheduled_time
- weekdays
- reminder_interval_minutes
- is_active
- created_at
- updated_at

---

### 9.1.3 タスク詳細取得

#### 用途
タスク編集画面に現在の設定を表示する。

#### 入力
- `task_id`

#### 出力
対象の`tasks`レコード1件。

#### エラー
- 対象タスクが存在しない場合はNot Found

---

### 9.1.4 タスク登録

#### 用途
タスク登録画面から新しいタスクを登録する。

#### 入力

```json
{
  "title": "朝の薬",
  "description": "朝食後",
  "schedule_type": "daily",
  "scheduled_date": null,
  "scheduled_time": "07:00",
  "weekdays": null,
  "reminder_interval_minutes": 5
}
```

#### 入力ルール

##### once
- `scheduled_date` 必須
- `scheduled_time` 必須
- `weekdays` はNULL

##### daily
- `scheduled_date` はNULL
- `scheduled_time` 必須
- `weekdays` はNULL

##### weekly
- `scheduled_date` はNULL
- `scheduled_time` 必須
- `weekdays` 必須

曜日値は以下。

- 0: 日
- 1: 月
- 2: 火
- 3: 水
- 4: 木
- 5: 金
- 6: 土

#### 登録時固定値
- `is_active = true`

#### 登録後処理
必要に応じて直近の`task_occurrences`を生成する。

---

### 9.1.5 タスク更新

#### 用途
タスク編集画面からタスク設定を更新する。

#### 入力
- `task_id`
- タスク名
- 説明
- 実行条件
- 通知間隔
- 有効／無効

#### 更新対象
`tasks`

#### 注意事項
実行条件を変更した場合、まだ実行時刻を迎えていない`pending`の`task_occurrences`については、新しい条件に合わせて再生成または更新する。

完了・スキップ済みの履歴は変更しない。

---

### 9.1.6 タスク完了

#### 用途
ホーム画面から対象タスクを完了する。

#### 入力
- `occurrence_id`

#### 更新内容

```text
status = completed
completed_at = 現在日時
next_notification_at = NULL
```

#### 後続処理
- Google Calendarへ実績イベントを登録する
- 登録成功時
  - `google_event_id` 更新
  - `calendar_synced_at` 更新
  - `calendar_sync_error = NULL`
- 登録失敗時
  - タスクの完了自体は成功扱い
  - `calendar_sync_error` にエラーを保存
  - 後続の再同期対象とする

---

### 9.1.7 タスクスキップ

#### 用途
ホーム画面から対象タスクをスキップする。

#### 入力
- `occurrence_id`

#### 更新内容

```text
status = skipped
skipped_at = 現在日時
next_notification_at = NULL
```

#### Google Calendar
登録しない。

---

### 9.1.8 履歴一覧取得

#### 用途
履歴画面に完了・スキップ済みの履歴を表示する。

#### 入力
- `search_text` 任意

#### 取得条件
- `status IN ('completed', 'skipped')`

#### 検索条件
`search_text`が指定された場合はタスク名に対する部分一致検索。

#### 並び順
処理日時の新しい順。

処理日時は以下とする。

```text
completed → completed_at
skipped   → skipped_at
```

#### 出力項目
- occurrence_id
- task_id
- task名
- scheduled_at
- status
- completed_at
- skipped_at
- Google Calendar同期状態

---

### 9.1.9 履歴詳細取得

#### 用途
履歴詳細画面に1件の実績を表示する。

#### 入力
- `occurrence_id`

#### 出力項目
- タスク名
- 説明
- 予定日時
- status
- 完了日時
- スキップ日時
- 完了時の予定との差
- Google Calendar同期状態
- Google Calendar同期エラー

---

### 9.1.10 Push購読登録

#### 用途
PWAでWeb Push通知を受信できるようにする。

#### 入力例

```json
{
  "endpoint": "https://...",
  "p256dh": "...",
  "auth": "..."
}
```

#### 処理
`push_subscriptions`へ登録する。

同じ`endpoint`が既に存在する場合は更新する。

---

### 9.1.11 テスト通知

#### 用途
設定画面からPush通知が正常に届くか確認する。

#### 入力
なし

#### 処理
現在登録されているPush Subscriptionへテスト通知を送信する。

#### 通知例

```text
実行支援
これはテスト通知です。
```

---

### 9.1.12 Google Calendar接続

#### 用途
Google CalendarとのOAuth連携を開始・再接続する。

#### 処理
- Google OAuth認証
- 必要な権限を取得
- refresh tokenを保存
- 登録先Calendarを取得または選択
- `app_settings`を更新

---

### 9.1.13 Google Calendar設定取得

#### 用途
設定画面にGoogle Calendar接続状態を表示する。

#### 出力例

```json
{
  "connected": true,
  "calendar_id": "xxxx@group.calendar.google.com"
}
```

refresh tokenはクライアントへ返さない。

---

### 9.1.14 Google Calendarテスト登録

#### 用途
設定画面からGoogle Calendar連携が正常か確認する。

#### 処理
指定Calendarへテストイベントを登録する。

#### イベント名

```text
実行支援 接続テスト
```

---

## 9.2 バックグラウンド・内部処理

### 9.2.1 task_occurrences生成

#### 用途
`tasks`の実行条件から、実際の1回分となる`task_occurrences`を生成する。

#### 対象
- `tasks.is_active = true`

#### 実行条件

##### once
指定された日付・時刻について1件生成する。

##### daily
毎日、指定時刻について生成する。

##### weekly
指定された曜日・時刻について生成する。

#### 初期値

```text
status = pending
scheduled_at = 対象日時
next_notification_at = scheduled_at
```

#### 重複防止
同一`task_id`、同一`scheduled_at`の`task_occurrences`を重複生成しない。

---

### 9.2.2 Push通知送信

#### 用途
実行時刻になった未完了タスクへ通知を送信する。

#### 対象条件

```text
status = pending
AND next_notification_at <= 現在日時
```

#### 処理
1. `push_subscriptions`の通知先へPush送信
   通知本文の予定時刻は JST (`Asia/Tokyo`) で表示する
2. 送信後、以下を更新

```text
next_notification_at =
  現在日時 + tasks.reminder_interval_minutes
```

#### 通知内容例

```text
朝の薬
07:00のタスクが未完了です。
```

#### 通知停止条件
以下のいずれかになった場合。

- completed
- skipped

---

### 9.2.3 Google Calendar実績登録

#### 用途
completedとなったタスクをGoogle Calendarへ記録する。

#### 対象条件

```text
status = completed
AND google_event_id IS NULL
```

#### イベント内容

タイトル:

```text
朝の薬
```

開始日時:
- `completed_at`

説明:

```text
予定日時: 2026/08/10 07:00
完了日時: 2026/08/10 07:08
```

#### 成功時

```text
google_event_id = Google Calendar Event ID
calendar_synced_at = 現在日時
calendar_sync_error = NULL
```

#### 失敗時

```text
calendar_sync_error = エラー内容
```

タスクの`completed`状態は戻さない。

---

### 9.2.4 Google Calendar同期再試行

#### 用途
Google Calendar登録に失敗した完了タスクを再同期する。

#### 対象条件

```text
status = completed
AND google_event_id IS NULL
AND calendar_sync_error IS NOT NULL
```

#### 処理
Google Calendar実績登録処理を再実行する。

#### 注意
同一タスクについて重複イベントを作成しないよう、Google Calendar登録処理は冪等性を考慮する。

---

### 9.2.5 無効タスクの扱い

`tasks.is_active = false`の場合、新しい`task_occurrences`を生成しない。

既に存在する未来の`pending` occurrenceについては、タスク無効化時に実行対象外とする。

MVPでは、無効化時に未来の`pending` occurrenceを削除してよい。

過去の`completed`および`skipped`は保持する。

---

## 9.3 エラー処理方針

### 入力エラー
- 必須項目不足
- 不正な実行条件
- 通知間隔が0以下
- 不正な曜日値

これらは画面へ入力エラーとして返す。

### データ未存在
対象のtaskまたはoccurrenceが存在しない場合はNot Foundとして扱う。

### 外部サービスエラー
以下はタスク本体の更新とは分離する。

- Web Push
- Google Calendar

Google Calendar登録失敗時でもタスク完了は取り消さない。

### 二重操作
同じoccurrenceに対して完了・スキップが複数回送信された場合でも状態が破損しないようにする。

`pending`からのみ`completed`または`skipped`へ遷移可能とする。

---

## 10. 現行実装補足

以下は現行コードに存在するが、上記要件に明示されていなかった実装前提である。

### 10.1 フロントエンド構成

- `index.html` + `app.js` + `styles.css` のシングルページアプリ構成
- 画面遷移は `#/home` などの hash routing を用いる
- 画面は以下の7画面を実装済み
  - ホーム
  - タスク一覧
  - タスク登録
  - タスク編集
  - 履歴
  - 履歴詳細
  - 設定

### 10.2 状態同期方式

- 現行MVPでは画面単位APIではなく、アプリ全体状態をまとめて同期する
- 取得APIは `GET /api/state`
- 保存APIは `POST /api/state`
- 同期対象は以下
  - `tasks`
  - `task_occurrences`
  - `app_settings` の一部
- クライアント側で状態を編集し、保存時に Supabase へ upsert する

### 10.3 task_occurrences の生成範囲

- occurrence は無制限には生成せず、現在日から30日先までを対象に生成する
- 生成対象期間はアプリ定数 `OCCURRENCE_WINDOW_DAYS = 30` で管理する
- 初回読込時にも不足分を補完し、その結果件数が変わった場合は自動保存する

### 10.4 タスク更新・無効化時の pending occurrence 再計算

- タスク登録・更新・有効化・無効化のたびに対象タスクの future/pending occurrence を再計算する
- 実行条件に合わなくなった pending occurrence は削除する
- `completed` と `skipped` の履歴は削除しない

### 10.5 ホーム画面の実表示条件

- ホーム画面に出すのは `pending` かつ対象タスクが `is_active = true` のものだけ
- 超過中一覧は `scheduled_at <= 現在時刻`
- 今日これから一覧は `scheduled_at > 現在時刻` かつ当日分のみ

### 10.6 履歴画面の実検索仕様

- 検索対象はタスク名の部分一致
- 大文字小文字は区別しない
- 並び順は `completed_at` または `skipped_at`、なければ `updated_at` を用いた降順

### 10.7 Google Calendar 連携の現状

- 要件上は Google Calendar 本番連携を想定しているが、現行実装はデモ状態である
- 設定画面の接続操作は OAuth 連携ではなく、接続状態フラグをローカル状態として保存する
- 完了時、接続済み扱いであれば `google_event_id` に `demo-xxxxxxxx` 形式のダミー値を保存する
- 未接続時は `calendar_sync_error` に未同期理由を保存する
- テスト登録も外部API呼び出しは行わず、実行記録のみ保持する

### 10.8 Push 通知の実装前提

- Push 通知は Web Push + Service Worker (`sw.js`) で実装する
- `GET /api/public-config` は VAPID 公開鍵と Push 利用可否を返す
- `POST /api/push-subscriptions` は `endpoint` 単位で upsert する
- `POST /api/push-test` は最新の購読先1件に対してテスト通知を送る
- Service Worker は Push 受信時、通知クリックで `/#/home` を開く

### 10.9 定刻通知・再通知の実行方式

- `GET /api/jobs-dispatch` を外部 Cron から定期的に呼び出す
- 対象は以下を満たす occurrence

```text
status = pending
AND next_notification_at <= 現在日時
AND tasks.is_active = true
```

- 通知送信後は次回通知日時を以下で更新する

```text
next_notification_at =
  直前の next_notification_at + reminder_interval_minutes
```

- 購読先が `404` または `410` を返した場合は、その subscription を削除する

### 10.10 Cron エンドポイントの保護

- `CRON_SECRET` が設定されている場合、`/api/jobs-dispatch` は以下のいずれかで認証する
  - `Authorization: Bearer <CRON_SECRET>`
  - `x-vercel-cron` ヘッダ

### 10.11 環境変数依存

- サーバーAPIは Supabase 利用のため以下を必須とする
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`
- Web Push 有効化にはさらに以下を必須とする
  - `VAPID_PUBLIC_KEY`
  - `VAPID_PRIVATE_KEY`
  - `VAPID_SUBJECT`
- 外部 Cron から呼び出す場合は `CRON_SECRET` を用いる

### 10.12 オフライン対応

- PWA として `manifest.webmanifest` と `sw.js` を持つ
- Service Worker は app shell をキャッシュし、主要静的資産はオフラインでも再利用可能
- ただしデータ更新APIはオンライン前提であり、完全なオフライン編集同期は未実装

---

## 11. 開発・動作確認記録

2026年8月10日までに実施したローカルデバッグ、Push通知確認、UI調整の内容を記録する。

### 11.1 ローカル起動方法

反復デバッグでは、`.env.local` を直接読み込むローカル専用サーバーを使用する。

```bash
npm install
npm run dev:local
```

- 既定URLは `http://localhost:3000`
- `scripts/dev-server.mjs` が静的ファイルと `api/*` の両方を配信する
- Vercel環境に近い動作を確認する場合は `npm run dev:vercel` または `npx vercel dev` を使用する
- `vercel dev` が `Requested port 3000 is already in use` と表示した場合は、`Ready!` に表示された実際のポートを使用する
- ブラウザと `curl` は必ず同じポートへ接続する
- 環境変数を変更した場合は開発サーバーを再起動する

起動確認は次の順で行う。

```bash
curl -i http://localhost:3000/api/public-config
curl -i http://localhost:3000/api/state
```

### 11.2 ローカルデバッグで判明したエラーと対応

| 症状・レスポンス | 原因 | 対応状況 |
|---|---|---|
| `curl: (7) Failed to connect` | サーバー未起動、終了済み、またはポート違い | `Ready!` のURLと実際の待受ポートを確認する |
| `SUPABASE_URL が設定されていません` | サーバーが有効なSupabase環境変数を読み込めていない | `.env.local` に設定し、サーバーを再起動する |
| Node.js 20のWebSocketエラー | Supabaseクライアントが利用するWebSocket実装がNode.js 20にない | `ws` を依存関係へ追加し、Supabaseクライアントのtransportに設定済み |
| `Invalid time value` | Supabaseの `time` 型が `HH:MM:SS` で返り、画面側で秒を重ねて日時を組み立てていた | 読込時に `HH:MM` へ正規化し、不正日時の表示ガードも実装済み |
| APIが読み込み中のままになる | APIが応答しない場合に画面側の待機が終わらなかった | API呼び出しに8秒のタイムアウトを実装済み |
| `/api/jobs-dispatch` が `401 Unauthorized` | `CRON_SECRET` 設定時に認証ヘッダーがない | `Authorization: Bearer <CRON_SECRET>` を付ける |
| `Vapid subject is not a valid URL` | `VAPID_SUBJECT` がメールアドレスのみになっている | `mailto:user@example.com` 形式にする |

### 11.3 環境変数とVAPID

ローカル環境変数のファイル名は `.env.local` とする。最低限、次を設定する。

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `VAPID_PUBLIC_KEY`
- `VAPID_PRIVATE_KEY`
- `VAPID_SUBJECT`
- `CRON_SECRET`

VAPIDはWeb Push送信元を識別し、Pushメッセージへ署名するために使用する。

- `VAPID_PUBLIC_KEY` はブラウザのPush購読作成に使用する公開鍵
- `VAPID_PRIVATE_KEY` はサーバーだけが保持する署名用秘密鍵
- `VAPID_SUBJECT` は運営者の連絡先であり、`mailto:自分のメールアドレス` または有効なHTTPS URLを設定する
- 秘密鍵、Supabase Service Role Key、`CRON_SECRET` は画面・文書・リポジトリへ公開しない

`GET /api/public-config` はPush設定の不足状況を返す。

- 必要なVAPID環境変数が揃っている場合は `pushSupported: true`、`pushMode: "remote"`
- 不足している場合は `pushSupported: false`、`pushMode: "local"` と不足変数名

### 11.4 Push通知の確認結果

以下の一連の動作をローカル環境で確認済みである。

1. Supabaseから空の状態を取得できる
2. 画面からタスクを登録・編集できる
3. ブラウザの通知権限を許可できる
4. Push購読情報をSupabaseへ保存できる
5. 設定画面で `Push: 購読済み`、`モード: 本番 Push` と表示される
6. 設定画面の「テスト通知」で通知を受信できる
7. 認証付きで `/api/jobs-dispatch` を手動実行し、対象タスクの通知を受信できる

`CRON_SECRET` をコマンド履歴や画面へ表示せず、手動ディスパッチを確認する場合は次を使用する。

```bash
read -s -p "CRON_SECRET: " CRON_SECRET_INPUT; echo
curl -i http://localhost:3000/api/jobs-dispatch \
  -H "Authorization: Bearer ${CRON_SECRET_INPUT}"
unset CRON_SECRET_INPUT
```

HTTP `200` が返り通知を受信できれば、Supabaseの対象取得、VAPID署名、Web Push送信、Service Worker受信まで動作している。

### 11.5 ローカル通知と本番Pushの違い

VAPID設定がない `pushSupported: false` の場合は、画面を開いている間だけローカル通知補助を使用する。

- 5秒ごとに期限到来した通知を確認する
- 起動時に過去の通知予定が蓄積している場合は、一括送信せず、現在時刻より後の次回通知枠まで繰り延べる
- ブラウザを閉じるとローカル通知補助は動作しない

VAPID設定がある `pushSupported: true` の場合は、本番Push経路を使用する。

- ブラウザでPush購読を作成する
- `/api/jobs-dispatch` が期限到来した `pending` occurrenceを抽出する
- ローカルでは認証付きAPI呼び出しで手動実行する
- 本番では外部 Cron が `jobs-dispatch` を定期実行する
- ブラウザを閉じていても、OS・ブラウザ側で通知が許可されていれば受信できる

### 11.6 現在のUI表示仕様

#### 共通ボタン

- 丸いピル形ではなく、角を適度に丸めた角型を使用する
- 主操作、補助操作、キャンセル系、危険操作を色と背景で区別する
- hover、押下、キーボードフォーカス、disabled状態を持つ

#### ホーム

- タスクカードは縦方向を圧縮した表示とする
- 説明は1行で省略表示できる
- 予定日時と通知間隔または超過時間を同じ行へまとめる
- 「未完了かつ予定時刻超過」内の「超過中」バッジは表示しない
- セクション見出し横の「古い順」「時刻順」は表示しない
- 内部の並び順処理は維持する

#### タスク一覧

- 「有効」「無効」の状態バッジは表示しない
- 有効タスクは淡い緑系、無効タスクはグレー系の背景と低彩度の文字で判別する
- 状態識別の実線・破線は使用しない
- 通知間隔はスケジュール情報と同じ行の右端へ表示する
- 狭い画面では通知間隔を自然に折り返す
- 読み上げ環境向けの有効・無効情報は `aria-label` に保持する

#### 履歴

- 検索テキストボックスに幅、最小幅、内側余白、外枠、角丸、背景色を明示する
- PC・スマートフォンの両方で外枠がカード内に収まるようにする

### 11.7 現時点の残作業

- Vercelへデプロイする
- `.env.local` と同じ種類の環境変数をVercelへ登録する
- 外部 Cron による自動通知を実環境で確認する
- ブラウザを閉じた状態で定刻通知と再通知を確認する
- Google Calendarの本番OAuth・イベント登録連携を実装する
